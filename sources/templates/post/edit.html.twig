{% extends 'layout.html.twig' %}

{% block body %}
    <h2>Edit post {{ post.name }}</h2>
    <div class="row">
        <div class="col-4">
            {{ form(postForm) }}
        </div>
    </div>
{% endblock %}